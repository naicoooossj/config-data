# ====================================
# REPORT SERVICE - DOCKER PROFILE
# (NO tiene base de datos propia)
# ====================================
server.port=0

# Eureka
eureka.instance.prefer-ip-address=true

# URLs de otros servicios para consultas (via Eureka/RestTemplate)
# No necesita configuración de BD
